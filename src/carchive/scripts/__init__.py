"""
Core scripts for the carchive system.

This module contains script implementations that are called by CLI commands
and are considered part of the core codebase, as opposed to one-off scripts
in the /scripts directory at the repository root.
"""